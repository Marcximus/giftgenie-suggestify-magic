import { ReactElement } from 'react';

export type Person = {
  label: string;
  icon: ReactElement;
};

export type AgeRange = {
  label: string;
  range: string;
};

export type PriceRange = {
  label: string;
  range: string;
};

export type Interest = {
  label: string;
  icon: ReactElement;
};