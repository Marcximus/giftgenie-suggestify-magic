export type Person = {
  label: string;
  icon: string;
};

export type AgeRange = {
  label: string;
  range: string;
};

export type PriceRange = {
  label: string;
  range: string;
};

export type Interest = {
  label: string;
  icon: string;
};